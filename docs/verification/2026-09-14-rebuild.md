# 新版重构本地验收

日期：2026-09-14。范围：全新 Astro + TypeScript 重建；不迁移旧文章，不兼容旧 Python 数据格式。此记录只证明本地实现与验证，不代表 GitHub Actions 或线上部署成功。

## 实现范围

- Node 24 工具链、严格类型和数据校验、统一代码格式。
- RSS/Atom 有界采集，逐源 JSON 持久数据、历史合并与持续撤回身份。
- 文章动态、博客目录、加入说明；每页 20 篇的静态页面和全局搜索筛选。
- 只读 PR 检查、离线代码发布、定时与手动刷新、受限数据写回、官方 GitHub Pages artifact 部署。
- 原工作区修改保存在忽略的 `.local/legacy-before-rebuild`；新程序不读取这些文件。

## 数据与自动验证

最终采集使用四个真实来源，成功取得 115 篇当前 Feed 条目：nyc 13、canyie 20、simuleite 20、toho 62。候选构建和产物检查成功后，四个快照才提升到 `data/feeds/`。没有从旧 `config.json` 导入记录。

- 35 项离线测试：日期、解析、合并、歧义、撤回/恢复、请求限制、故障回退、搜索与发布数据一致性。
- Astro 检查：35 个文件，0 错误、0 警告、0 提示。
- 根路径 `/`（`https://baiyunu.nyc1.xyz`）和项目路径 `/BaiyunUBlogroll/` 的生产构建与 `check:dist`。
- 8 个静态 HTML 页面、过滤后的 `catalog.json` 与真实 `status.json`；资源、canonical、分页路径完整。
- 刷新数据与刷新报告必须同时匹配待发布产物，不能以相同文章数据为由混用不同轮次的检查时间。
- 最后交付的本地预览是离线构建，`status.json` 标记 `not_checked`；不会把构建时间伪装成来源检查时间。

## 浏览器验证

使用 Codex 内置浏览器访问生产静态产物：

- 搜索 `nginx` 找到首页以外的唯一文章；`Linux 安全` 多词输入保留空格并返回正确结果。
- 西行妖筛选显示 62 篇；第二页显示 21–40，刷新 `?source=toho&page=2` 后保持相同结果。
- 无匹配查询显示空状态和 0 条；清除恢复列表。
- 320px 窄屏首页、目录和加入页无横向溢出。修复了 Windows 竖向滚动条导致的最小宽度溢出，并使首页标题按语句换行。
- 搜索输入有可见键盘焦点；常规预览无浏览器错误或警告。
- 临时本地测试服务器通过 CSP 禁止脚本执行后，文章与真实静态分页仍可阅读；第一页到第二页显示 21–40 条。页面另提供 `noscript` 提示。
- 临时测试样式选择现有深色配色，目视检查背景、卡片、链接和文字对比；未更改用户系统主题。
- 博客目录计数与数据一致，加入按钮指向该仓库 GitHub Issue 入口。

## 独立审查与边界

两项独立审查覆盖数据/发布脚本与页面/工作流。发现并修复：零有效采集被历史记录掩盖、刷新报告与产物不一致、手动任务缺少默认分支限制、运行摘要缺失、搜索输入键盘焦点不可见。审查者已针对修改复核，无剩余实质问题。

只读 GitHub API 核实默认分支为 `master`；线上 Pages 仍为旧 `gh-pages` 分支发布，绑定 `baiyunu.nyc1.xyz`。该域名 HTTPS 请求返回 200。新工作流读取 Pages 的实际 origin/base，继续使用 GitHub Pages；首次上线需将 Pages Source 切换为 GitHub Actions。此轮没有修改远程设置、推送分支或执行部署，分支保护与真实 Actions 执行结果尚未验证。
